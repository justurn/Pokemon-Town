// Create Event of OBJ_Building
flashing = false;      // Flag to track whether the building is flashing
just_created = true; // Flag to track whether the building was just spawned for it to look up a sprite.

build_left = x;    // Left edge of the building
build_right = x;   // Right edge of the building

// Define colors for flashing
flash_color = c_red; // Color used for the flashing effect (you can adjust this to your preference)
normal_color = c_white; // Normal color of the building (could be the original sprite color)


